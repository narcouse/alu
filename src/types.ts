export interface ServiceItem {
  id: string;
  title: string;
  description: string;
  iconName: string;
}

export interface ContactInfo {
  whatsappUrl: string;
  instagramUrl: string;
  googleMapUrl: string;
  phone: string;
  location: string;
}
